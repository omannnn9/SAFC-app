const $=id=>document.getElementById(id);let googleClientId='',currentSource='welcomect',currentUser='';async function api(path,options={}){const res=await fetch(path,{headers:{'Content-Type':'application/json',...(options.headers||{})},credentials:'same-origin',...options});const type=res.headers.get('content-type')||'';const data=type.includes('application/json')?await res.json():await res.text();if(!res.ok)throw new Error((data&&data.error)||data||`Request failed ${res.status}`);return data}function showLoginHelp(msg){$('login-help').textContent=msg;}window.handleCredentialResponse=async(resp)=>{try{const data=await api('/api/admin/google-login',{method:'POST',body:JSON.stringify({credential:resp.credential})});showLoginHelp(`Signed in as ${data.username}.`);await refresh();}catch(err){showLoginHelp(`${err.message} Use a different Google account and choose your @southafricafc.com email.`);try{google.accounts.id.disableAutoSelect();}catch(e){} }};function renderGoogleButton(){if(!window.google||!google.accounts||!googleClientId)return;google.accounts.id.initialize({client_id:googleClientId,callback:handleCredentialResponse,auto_select:false,cancel_on_tap_outside:false,use_fedcm_for_prompt:false});const holder=document.querySelector('.g_id_signin');holder.innerHTML='';google.accounts.id.renderButton(holder,{type:'standard',size:'large',theme:'filled_black',text:'signin_with',shape:'pill'});}async function initGoogle(){const cfg=await api('/api/watchparty/config?source=main');googleClientId=cfg.google_client_id;const chooser=`https://accounts.google.com/AccountChooser?continue=${encodeURIComponent(location.href)}`;$('account-chooser-link').href=chooser;if(!googleClientId){showLoginHelp('Google sign-in is not configured yet. Set SAFC_GOOGLE_CLIENT_ID in the deployment environment.');return;}showLoginHelp('Use your authorized @southafricafc.com Google account. If your personal account is rejected, choose a different Google account.');let tries=0;const wait=setInterval(()=>{tries++;if(window.google&&google.accounts){clearInterval(wait);renderGoogleButton();}else if(tries>40){clearInterval(wait);showLoginHelp('Google sign-in script did not load. Refresh and try again.');}},100)}async function refresh(){try{const session=await api('/api/session');currentUser=session.username||'';$('session').classList.toggle('hidden',!session.authenticated);$('session').textContent=session.authenticated?`Signed in as ${session.username}`:'Not signed in';$('login-box').classList.toggle('hidden',session.authenticated);$('super-admin').classList.toggle('hidden',session.username!=='kj@southafricafc.com');if(session.authenticated){await loadSummary();await loadList(currentSource);}}catch(e){console.warn(e)}}async function loadSummary(){const data=await api('/api/watchparty/admin/summary');$('summary').innerHTML='';data.items.forEach(item=>{const b=document.createElement('button');b.className='secondary';b.textContent=`${item.label}: ${item.signups} sign-ups • ${item.winners} winner(s)`;b.onclick=()=>loadList(item.source_key);$('summary').appendChild(b);});}async function loadList(source){currentSource=source;const data=await api(`/api/watchparty/admin/signups?source=${encodeURIComponent(source)}`);$('list-title').textContent=`${data.label} sign-ups (${data.items.length})`;$('signup-list').innerHTML=data.items.map(i=>`<tr><td>${i.full_name}</td><td>${i.email}</td><td>${i.mobile}</td><td>${i.payment_reference}</td><td>${i.membership_status}</td><td>${i.created_at}</td></tr>`).join('')||'<tr><td colspan="6">No sign-ups yet.</td></tr>';}document.querySelectorAll('[data-source]').forEach(b=>b.onclick=()=>loadList(b.dataset.source));$('choose-account').onclick=()=>{try{google.accounts.id.disableAutoSelect();google.accounts.id.prompt();showLoginHelp('Choose your @southafricafc.com Google account. If the browser reuses the wrong account, use the account chooser link.');}catch(e){showLoginHelp('Open the Google account chooser link, switch account, then return and sign in again.');}};$('pick-winner').onclick=async()=>{try{const data=await api('/api/watchparty/admin/random-winner',{method:'POST',body:JSON.stringify({source_key:currentSource,prize_label:$('prize-label').value})});const w=data.winner;$('winner').classList.remove('hidden');$('winner').textContent=`Winner: ${w.full_name} • ${w.email} • ${w.mobile} • ${w.payment_reference}. Email tracker: ${data.email_sent?'sent':'not sent'}${data.email_error?' — '+data.email_error:''}`;await loadSummary();}catch(err){alert(err.message);}};$('create-admin').onclick=async()=>{try{const email=$('new-admin').value.trim();const data=await api('/api/watchparty/admin/create-admin',{method:'POST',body:JSON.stringify({email})});alert(`Authorized ${data.email}`);}catch(err){alert(err.message);}};$('logout').onclick=async()=>{try{google.accounts.id.disableAutoSelect();}catch(e){}await api('/api/admin/logout',{method:'POST',body:'{}'});location.reload();};initGoogle().then(refresh);